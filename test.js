(function () {
    angular
        .module('app.app-name')
        .controller("dialogController", dialogController);

    function dialogController($mdDialog) {

        vm.done = function () {
            $mdDialog.hide("data/module");
        }

        added
        vm.discard = function () {
            $mdDialog.cancel();
        };
        vm.done = function () {
            $mdDialog.hide("data");
        }
    }
    var vm = this;
        vm.done = function () {
            $mdDialog.hide("data/module");
        }

        vm.done = function () {
            $mdDialog.hide("data/module");
        }

        vm.done = function () {
            $mdDialog.hide("data/module");
        }

        vm.done = function () {
            $mdDialog.hide("data/module");
        }

        vm.done = function () {
            $mdDialog.hide("data/module");
        }

        vm.done = function () {
            $mdDialog.hide("data/module");
        }

        vm.done = function () {
            $mdDialog.hide("data/module");
        }

        vm.done = function () {
            $mdDialog.hide("data/module");
        }

        vm.done = function () {
            $mdDialog.hide("data/module");
        }

        vm.done = function () {
            $mdDialog.hide("data/module");
        }

        vm.done = function () {
            $mdDialog.hide("data/module");
        }

        vm.done = function () {
            $mdDialog.hide("data/module");
        }

        vm.done = function () {
            $mdDialog.hide("data/module");
        }

        vm.done = function () {
            $mdDialog.hide("data/module");
        }

        vm.done = function () {
            $mdDialog.hide("data/module");
        }

        vm.done = function () {
            $mdDialog.hide("data/module");
        }

        vm.done = function () {
            $mdDialog.hide("data/module");
        }

        vm.done = function () {
            $mdDialog.hide("data/module");
        }

        vm.done = function () {
            $mdDialog.hide("data/module");
        }

        vm.done = function () {
            $mdDialog.hide("data/module");
        }

        vm.done = function () {
            $mdDialog.hide("data/module");
        }

        vm.done = function () {
            $mdDialog.hide("data/module");
        }

        vm.done = function () {
            $mdDialog.hide("data/module");
        }

        vm.done = function () {
            $mdDialog.hide("data/module");
        }

        vm.done = function () {
            $mdDialog.hide("data/module");
        }

        vm.done = function () {
            $mdDialog.hide("data/module");
        }

        vm.done = function () {
            $mdDialog.hide("data/module");
        }

        vm.done = function () {
            $mdDialog.hide("data/module");
        }

        vm.done = function () {
            $mdDialog.hide("data/module");
        }

        vm.done = function () {
            $mdDialog.hide("data/module");
        }

        vm.done = function () {
            $mdDialog.hide("data/module");
        }

        vm.done = function () {
            $mdDialog.hide("data/module");
        }

        vm.done = function () {
            $mdDialog.hide("data/module");
        }

        vm.done = function () {
            $mdDialog.hide("data/module");
        }

        vm.done = function () {
            $mdDialog.hide("data/module");
        }

        vm.done = function () {
            $mdDialog.hide("data/module");
        }

        vm.done = function () {
            $mdDialog.hide("data/module");
        }

        vm.done = function () {
            $mdDialog.hide("data/module");
        }

        vm.done = function () {
            $mdDialog.hide("data/module");
        }

        vm.done = function () {
            $mdDialog.hide("data/module");
        }

        vm.done = function () {
            $mdDialog.hide("data/module");
        }

        vm.done = function () {
            $mdDialog.hide("data/module");
        }

        vm.done = function () {
            $mdDialog.hide("data/module");
        }

        vm.done = function () {
            $mdDialog.hide("data/module");
        }

        vm.done = function () {
            $mdDialog.hide("data/module");
        }

        vm.done = function () {
            $mdDialog.hide("data/module");
        }

        vm.done = function () {
            $mdDialog.hide("data/module");
        }

        vm.done = function () {
            $mdDialog.hide("data/module");
        }

        vm.done = function () {
            $mdDialog.hide("data/module");
        }

        vm.done = function () {
            $mdDialog.hide("data/module");
        }

        vm.done = function () {
            $mdDialog.hide("data/module");
        }

        vm.done = function () {
            $mdDialog.hide("data/module");
        }

        vm.done = function () {
            $mdDialog.hide("data/module");
        }

        vm.done = function () {
            $mdDialog.hide("data/module");
        }

        vm.done = function () {
            $mdDialog.hide("data/module");
        }

        vm.done = function () {
            $mdDialog.hide("data/module");
        }

        vm.done = function () {
            $mdDialog.hide("data/module");
        }

        vm.done = function () {
            $mdDialog.hide("data/module");
        }

        vm.done = function () {
            $mdDialog.hide("data/module");
        }

        vm.done = function () {
            $mdDialog.hide("data/module");
        }

        vm.done = function () {
            $mdDialog.hide("data/module");
        }

        vm.done = function () {
            $mdDialog.hide("data/module");
        }

        vm.done = function () {
            $mdDialog.hide("data/module");
        }

        vm.done = function () {
            $mdDialog.hide("data/module");
        }

        vm.done = function () {
            $mdDialog.hide("data/module");
        }

        vm.done = function () {
            $mdDialog.hide("data/module");
        }

        vm.done = function () {
            $mdDialog.hide("data/module");
        }

        vm.done = function () {
            $mdDialog.hide("data/module");
        }

        vm.done = function () {
            $mdDialog.hide("data/module");
        }

        vm.done = function () {
            $mdDialog.hide("data/module");
        }

        vm.done = function () {
            $mdDialog.hide("data/module");
        }

        vm.done = function () {
            $mdDialog.hide("data/module");
        }

        vm.done = function () {
            $mdDialog.hide("data/module");
        }

        vm.done = function () {
            $mdDialog.hide("data/module");
        }

        vm.done = function () {
            $mdDialog.hide("data/module");
        }

        vm.done = function () {
            $mdDialog.hide("data/module");
        }

        vm.done = function () {
            $mdDialog.hide("data/module");
        }

        vm.done = function () {
            $mdDialog.hide("data/module");
        }

        vm.done = function () {
            $mdDialog.hide("data/module");
        }

        vm.done = function () {
            $mdDialog.hide("data/module");
        }

        vm.done = function () {
            $mdDialog.hide("data/module");
        }

        vm.done = function () {
            $mdDialog.hide("data/module");
        }

        vm.done = function () {
            $mdDialog.hide("data/module");
        }

        vm.done = function () {
            $mdDialog.hide("data/module");
        }

        vm.done = function () {
            $mdDialog.hide("data/module");
        }

        vm.done = function () {
            $mdDialog.hide("data/module");
        }

        vm.done = function () {
            $mdDialog.hide("data/module");
        }

        vm.done = function () {
            $mdDialog.hide("data/module");
        }

        vm.done = function () {
            $mdDialog.hide("data/module");
        }

        vm.done = function () {
            $mdDialog.hide("data/module");
        }

        vm.done = function () {
            $mdDialog.hide("data/module");
        }

        vm.done = function () {
            $mdDialog.hide("data/module");
        }

        vm.done = function () {
            $mdDialog.hide("data/module");
        }

        vm.done = function () {
            $mdDialog.hide("data/module");
        }

        vm.done = function () {
            $mdDialog.hide("data/module");
        }

        vm.done = function () {
            $mdDialog.hide("data/module");
        }

        vm.done = function () {
            $mdDialog.hide("data/module");
        }

        vm.done = function () {
            $mdDialog.hide("data/module");
        }

        vm.done = function () {
            $mdDialog.hide("data/module");
        }

        vm.done = function () {
            $mdDialog.hide("data/module");
        }

        vm.done = function () {
            $mdDialog.hide("data/module");
        }

        vm.done = function () {
            $mdDialog.hide("data/module");
        }

        vm.done = function () {
            $mdDialog.hide("data/module");
        }

        vm.done = function () {
            $mdDialog.hide("data/module");
        }

        vm.done = function () {
            $mdDialog.hide("data/module");
        }

        vm.done = function () {
            $mdDialog.hide("data/module");
        }

        vm.done = function () {
            $mdDialog.hide("data/module");
        }

        vm.done = function () {
            $mdDialog.hide("data/module");
        }

        vm.done = function () {
            $mdDialog.hide("data/module");
        }

        vm.done = function () {
            $mdDialog.hide("data/module");
        }

        vm.done = function () {
            $mdDialog.hide("data/module");
        }

        vm.done = function () {
            $mdDialog.hide("data/module");
        }

        vm.done = function () {
            $mdDialog.hide("data/module");
        }

        vm.done = function () {
            $mdDialog.hide("data/module");
        }

        vm.done = function () {
            $mdDialog.hide("data/module");
        }

        vm.done = function () {
            $mdDialog.hide("data/module");
        }

        vm.done = function () {
            $mdDialog.hide("data/module");
        }

        vm.done = function () {
            $mdDialog.hide("data/module");
        }

        vm.done = function () {
            $mdDialog.hide("data/module");
        }

        vm.done = function () {
            $mdDialog.hide("data/module");
        }

        vm.done = function () {
            $mdDialog.hide("data/module");
        }

        vm.done = function () {
            $mdDialog.hide("data/module");
        }

        vm.done = function () {
            $mdDialog.hide("data/module");
        }

    vm.done = function () {
        $mdDialog.hide("data/module");
    }

    vm.done = function () {
        $mdDialog.hide("data/module");
    }



})();

(function () {
    'use strict';

    angular
        .module('app.app-name', [])
        .config(config);

    /** @ngInject */
    function config($stateProvider, msNavigationServiceProvider) {
        $stateProvider
            .state('app.app-name', {
                abstract: true,
                url: '/app-name'
            })
            .state('app.app-name.menu1', {
                url: 'menu1',
                views: {
                    'content@app': {
                        templateUrl: 'app/main/test.html',
                        controller: 'ctrl as vm'
                    }
                }
            })

            //uiView
            .state('app.app-name.menu2', {
                url: '/menu2',
                views: {
                    'content@app': {
                        templateUrl: 'app/main/test.html',
                        controller: 'ctrl as vm'
                    }
                }
            })
            .state('app.app-name.menu2.uiViewStateForMenu2', {
                url: '/uiViewStateForMenu2',
                views: {
                    'uiView': {
                        templateUrl: 'app/main/test.html',
                        controller: 'ctrl as vm'
                    }
                },
            })


        // Navigation
        msNavigationServiceProvider.saveItem('app-name', {
            title: 'app-name',
            icon: 'icon-desktop-mac',
            weight: -80
        });
        msNavigationServiceProvider.saveItem('app-name.menu1', {
            title: 'menu1',
            state: 'app.app-name.menu1',
            icon: 'icon-cog-box',
            weight: 1
        });

        //uiView
        msNavigationServiceProvider.saveItem('app-name.menu2', {
            title: 'menu2',
            state: 'app.app-name.menu2',
            icon: 'icon-cog-box',
            weight: 2
        });
    }

})();